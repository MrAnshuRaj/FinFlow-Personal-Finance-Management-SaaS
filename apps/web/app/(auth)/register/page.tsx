"use client";
import Link from "next/link";
import { useRouter } from "next/navigation";
import { useState } from "react";
import { ArrowRight, CheckCircle2 } from "lucide-react";
import { api } from "@/lib/api";
export default function Register(){const r=useRouter(),[form,setForm]=useState({name:"",email:"",password:""}),[error,setError]=useState(""),[loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError("");try{const result=await api<{accessToken:string;refreshToken:string}>("/auth/register",{method:"POST",body:JSON.stringify(form)});localStorage.setItem("finflow_access",result.accessToken);localStorage.setItem("finflow_refresh",result.refreshToken);r.push("/dashboard");}catch(e){setError(e instanceof Error?e.message:"We couldn’t create your account. Try again.");}finally{setLoading(false)}}return <main className="grid min-h-screen bg-[var(--bg)] lg:grid-cols-2"><section className="relative hidden overflow-hidden bg-[linear-gradient(145deg,#102d24,#17644f)] p-12 text-white lg:flex lg:flex-col"><div className="absolute -bottom-24 -left-20 h-96 w-96 rounded-full bg-[#71dcb8]/20 blur-3xl"/><Link href="/" className="relative flex items-center gap-2 text-xl font-extrabold tracking-[-.05em]"><span className="grid h-8 w-8 place-items-center rounded-[10px] bg-[#64dcb2] text-[#13382c]">↗</span>Fin<span className="text-[#71dcb8]">Flow</span></Link><div className="relative my-auto max-w-md"><p className="text-xs font-bold uppercase tracking-[.16em] text-[#8ee8c5]">Your financial space</p><h1 className="mt-4 text-5xl font-extrabold leading-[1.01] tracking-[-.06em]">Start with one clear picture.</h1><p className="mt-5 max-w-sm leading-7 text-[#c3ddd3]">Build a better system for your accounts, spending, and every plan ahead.</p><div className="mt-10 space-y-3">{["Keep accounts in one clear view","Understand your spending patterns","Make room for the goals that matter"].map(x=><p className="flex items-center gap-2 text-sm text-[#d0e8de]" key={x}><CheckCircle2 size={17} className="text-[#7ce3b9]"/>{x}</p>)}</div></div><p className="relative text-xs text-[#92b7aa]">No banking integration required. Your data stays in your workspace.</p></section><section className="grid place-items-center p-6"><form onSubmit={submit} className="w-full max-w-[390px] app-fade"><Link href="/" className="flex items-center gap-2 text-xl font-extrabold tracking-[-.05em] lg:hidden"><span className="grid h-8 w-8 place-items-center rounded-[10px] bg-[var(--brand)] text-white">↗</span>Fin<span className="text-[var(--brand)]">Flow</span></Link><p className="mt-10 text-[11px] font-extrabold uppercase tracking-[.14em] text-[var(--brand)]">Create your workspace</p><h1 className="mt-3 text-3xl font-extrabold tracking-[-.055em]">Set up your FinFlow</h1><p className="mt-2 text-sm text-[var(--muted)]">A focused financial space is a few details away.</p>{error&&<p className="mt-5 rounded-xl border border-red-200 bg-red-50 p-3 text-sm text-red-700">{error}</p>}{([['name','Your name','text'],['email','Email address','email'],['password','Password','password']] as const).map(([key,label,type])=><label key={key} className="mt-5 block text-sm font-bold">{label}<input className="input mt-2 h-11" type={type} value={form[key]} onChange={e=>setForm({...form,[key]:e.target.value})} required minLength={key==='password'?8:undefined} placeholder={key==='password'?'At least 8 characters':undefined}/>{key==='password'&&<span className="mt-1.5 block text-xs font-normal text-[var(--muted)]">Use at least 8 characters.</span>}</label>)}<button className="btn btn-primary mt-7 flex w-full items-center justify-center gap-2 py-3" disabled={loading}>{loading?"Creating your workspace…":"Create your account"}<ArrowRight size={16}/></button><p className="mt-7 text-center text-sm text-[var(--muted)]">Already use FinFlow? <Link className="font-bold text-[var(--brand)]" href="/login">Sign in</Link></p></form></section></main>}
